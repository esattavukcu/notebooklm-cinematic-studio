streamlit>=1.32,<2
playwright>=1.45,<2
python-dotenv>=1.0,<2
# Azure Blob upload (harvest Phase 3) — opsiyonel.
# AZURE_STORAGE_CONNECTION_STRING env var set edilmediyse import edilmez.
# Disk yeri tasarrufu için kaldırmak istersen yorum yapabilirsin.
azure-storage-blob>=12.19,<13
# Text gen: gemini_client.py iki mod destekler.
# - API mode (önerilen): google-genai SDK + GEMINI_API_KEY env.
#   Subprocess yok, daha hızlı, native rate-limit.
# - CLI fallback: API key yoksa gemini-cli (Node.js) OAuth subprocess yolu.
google-genai>=0.5,<2

# NotebookLM submit + video download: teng-lin/notebooklm-py.
# tmc/nlm Go CLI + Playwright harvest cycle'ını değiştirdi.
# Mevcut chrome_profiles/<id>/auth.json'ı doğrudan kullanır.
notebooklm-py>=0.4,<1

# Bulk import (admin "Drive Toplu" sekmesi):
# - gdown: public Drive klasöründen API key'siz dosya indirme
# - python-docx: .docx → plain text parse
# Admin paneldən 40+ docx'i otomatik job'a çeviren akışta kullanılır.
gdown>=5.0,<7
python-docx>=1.0,<2
