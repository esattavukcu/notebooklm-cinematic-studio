streamlit>=1.32,<2
playwright>=1.45,<2
python-dotenv>=1.0,<2
# Azure Blob upload (harvest Phase 3) — opsiyonel.
# AZURE_STORAGE_CONNECTION_STRING env var set edilmediyse import edilmez.
# Disk yeri tasarrufu için kaldırmak istersen yorum yapabilirsin.
azure-storage-blob>=12.19,<13
# Text gen artık Google'ın gemini-cli (Node.js) üzerinden çalışıyor —
# OAuth sign-in, API key gerekmez. Python tarafı sadece subprocess wrapper
# (gemini_client.py), ek Python dependency yok.
# (Eski 'openai>=1.0,<2' kaldırıldı — OpenRouter desteği removed.)
